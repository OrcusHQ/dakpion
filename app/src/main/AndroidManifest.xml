<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Needed to read which SIM (subscription/slot) an SMS arrived on, for SIM filtering. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <!-- Expedited WorkManager sync runs as a short foreground service on Android 8–11. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <!-- Keep-alive service: keeps the process alive so the SMS receiver fires
         on phones whose OEM battery manager force-stops background apps. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Direct "allow Dakpion to run unrestricted" prompt from the Device tab.
         Core function (payment SMS forwarding) breaks without it. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- OEM "Autostart" managers we deep-link to from the Device tab (package
         visibility, Android 11+). Absent packages are simply skipped. -->
    <queries>
        <package android:name="com.transsion.phonemaster" />
        <package android:name="com.transsion.phonemanager" />
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.oppo.safe" />
        <package android:name="com.oneplus.security" />
        <package android:name="com.vivo.permissionmanager" />
        <package android:name="com.iqoo.secure" />
        <package android:name="com.huawei.systemmanager" />
        <package android:name="com.samsung.android.lool" />
        <package android:name="com.samsung.android.sm_cn" />
    </queries>


    <application
        android:name=".DakpionApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dakpion"
        tools:targetApi="31">
        <!-- excludeFromRecents: on Infinix/Xiaomi/etc. swiping an app out of
             Recent apps force-stops it, after which no SMS can wake it. If
             Dakpion never appears in Recents, it can't be swiped away. It still
             opens from the launcher icon and the notification. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Dakpion"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove" />

        <!-- Read-only diagnostics trail (see DiagProvider). -->
        <provider
            android:name=".DiagProvider"
            android:authorities="${applicationId}.diag"
            android:exported="true" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Max priority: SMS_RECEIVED is an ordered broadcast and apps above us
             (Truecaller etc.) can abort it before we see the SMS. -->
        <receiver android:name=".SmsReceiver"
            android:exported="true">
            <intent-filter android:priority="2147483647" >
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Restart protection after reboot / app update. -->
        <receiver
            android:name=".SystemEventReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- FCM wake-up (see DakpionMessagingService). -->
        <service
            android:name=".DakpionMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Persistent keep-alive service (see SmsKeepAliveService). -->
        <service
            android:name=".SmsKeepAliveService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the SMS receiver alive so incoming mobile-banking payment SMS (bKash, Nagad, Rocket) are forwarded to the merchant's OrcusPay dashboard in real time for payment verification." />
        </service>

        <!-- Declare the type for WorkManager's foreground service (used only
             for expedited work on Android 8–11); required on Android 14+. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>

</manifest>
